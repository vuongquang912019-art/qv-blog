import { getSupabase } from '@/lib/supabase'
export const dynamic='force-dynamic'; export const revalidate=60
type Post={id:string;title:string;category:string;excerpt:string;cover_gradient:string;read_time:string;content:string}
async function getPosts():Promise<Post[]>{const s=getSupabase(); if(!s) return []; try{const {data}=await s.from('posts').select('*').order('published_at',{ascending:false}); return (data as Post[])||[]}catch{return []}}
export default async function Home(){const posts=await getPosts(); return(<main className="min-h-screen"><header className="max-w-[1200px] mx-auto px-6 lg:px-8 py-8 flex justify-between items-center"><div className="flex items-center gap-3"><div className="w-10 h-10 rounded-full bg-black text-white flex items-center justify-center font-serif font-bold text-xl">QV</div><div><div className="font-serif text-xl leading-none font-semibold tracking-tight">Quang Vương</div><div className="text-[11px] tracking-widest opacity-60 uppercase mt-0.5">Đức Nội • Hà Nội • 2026</div></div></div><a href="/admin" className="text-sm opacity-60 hover:opacity-100">Admin →</a></header><section className="max-w-[1200px] mx-auto px-6 lg:px-8 pt-8 pb-16"><div className="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-white shadow-sm text-[11px] tracking-widest uppercase mb-8"><span className="w-2 h-2 rounded-full bg-green-500 animate-pulse"></span>Đang sống chậm tại Đức Nội • {posts.length} bài viết</div><h1 className="font-serif text-[56px] lg:text-[84px] leading-[0.85] tracking-[-0.03em] max-w-4xl">Sống chậm,<br/><span className="opacity-60">làm kỹ.</span></h1><p className="mt-8 text-[18px] leading-relaxed opacity-70 max-w-xl">Góc nhỏ ghi lại hành trình tối giản, làm việc sâu và xây hệ thống ghi chú cho người lười nhưng muốn hiệu quả.</p></section><section className="max-w-[1200px] mx-auto px-6 lg:px-8 pb-24"><div className="flex items-baseline justify-between mb-10"><h2 className="font-serif text-4xl">Bài viết mới</h2><span className="text-sm opacity-50">{posts.length} bài</span></div>{posts.length===0?(<div className="clay-card p-10 text-center text-sm opacity-60">Chưa có bài viết.</div>):(<div className="grid md:grid-cols-3 gap-8">{posts.map(p=>(<article key={p.id} className="clay-card p-6 group"><div className={`h-[180px] rounded-[20px] mb-6 bg-gradient-to-br ${p.cover_gradient} relative overflow-hidden`}><div className="absolute inset-0 bg-black/0 group-hover:bg-black/5 transition-colors"/></div><div className="flex items-center gap-2 text-[11px] tracking-widest uppercase opacity-50 mb-3"><span>{p.category}</span><span>•</span><span>{p.read_time}</span></div><h3 className="font-serif text-[22px] leading-tight">{p.title}</h3><p className="mt-3 text-[14px] leading-relaxed opacity-60 line-clamp-3">{p.excerpt}</p></article>))}</div>)}</section><footer className="max-w-[1200px] mx-auto px-6 lg:px-8 py-12 border-t border-black/10 flex justify-between text-sm opacity-50"><div>© 2026 Quang Vương</div><div>Sống chậm, làm kỹ.</div></footer></main>) }
